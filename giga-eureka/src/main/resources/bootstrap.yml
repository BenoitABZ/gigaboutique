spring:
  application:
    name: giga-eureka
  cloud:
    enabled: true
    config:
      uri: http://localhost:9101
      failFast: true
      retry:
        maxAttempts: 20

management:
  security:
    enabled: false
